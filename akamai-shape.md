### When URL / path is changed in endpoints
  Have Akamai to add the corresponding config to allow the URL / path to go through.
  Have Shape to add the corresponding config to allow the URL / path to go through.
  
